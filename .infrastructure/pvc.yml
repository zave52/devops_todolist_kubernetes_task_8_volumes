apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-data
  namespace: todoapp
spec:
  storageClassName: pv-data
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
